<%= render 'pipelines/show_one' %>
<%= render 'show_one' %>

<div class="col-sm-2"></div>
<div class="col-sm-10">
<nav class="navbar navbar-default">
  <div class="container-fluid">
	<ul class="nav navbar-nav">
		<li><%= link_to 'Edit DRA Region', edit_pipeline_dra_path(@dra.pipeline_id, @dra.id) %> </li>
		<li><%= link_to 'Delete', pipeline_dra_path(@dra.pipeline_id, @dra.id), method: :delete, data: { confirm: 'Are you sure you want to delete this DRA region?' } %> </li>
		<li><%= link_to 'Back', :back %> </li>
	</ul>
  </div>
</nav>
</div>
