<p id="notice"><%= notice %></p>
<%= render 'pipelines/show_one' %>
<br>
<%= render 'stations/index' %>
<%= render 'stations/show_one' %>

<h2>Pump Unit</h2>
<table class="fixed", style="width: 860px;">
  <thead>
    <tr>
	  <th>Row</th>
      <th>Column</th>
	  <th>Pump ID</th>
	  <th></th>
    </tr>
  </thead>

  <tbody>
      <tr>
		<td><%= @unit.unit_row %></td>
        <td><%= @unit.unit_column %></td>
		<td><%= @unit.pump_id %></td>
		<td></td>
      </tr>
  </tbody>
</table>

<%= link_to 'Edit', edit_pipeline_station_unit_path(@pipeline, @station, @unit) %> |
<%= link_to 'Delete', pipeline_station_unit_path(@pipeline, @station, @unit), method: :delete, data: { confirm: 'Are you sure?' } %> |
<%= link_to 'Back', pipeline_station_path(@pipeline, @station) %>
