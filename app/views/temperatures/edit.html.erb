<%= render 'pipelines/show_one' %>
<br>
<%= render 'form' %>

<%= link_to 'Show', pipeline_temperature_path(@temperature.pipeline_id, @temperature.id) %> | 
<%= link_to 'Back', pipeline_path(@pipeline) %>
