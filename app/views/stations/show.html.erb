<p id="notice"><%= notice %></p>
<%= render 'pipelines/show_one' %>
<br>
<%= render 'index' %>
<%= render 'show_one' %>

<%= link_to 'Edit Station', edit_pipeline_station_path(@station.pipeline_id, @station.id) %> |
<%= link_to 'Delete', pipeline_station_path(@station.pipeline_id, @station.id), method: :delete, data: { confirm: 'Are you sure you want to delete this station?' } %> |
<%= link_to 'Back', pipeline_path(@pipeline) %>

<%= render 'units/index' %>




