<%= render 'pipelines/show_one' %>
<%= render 'form' %>

<%= link_to 'Show', pipeline_segment_path(@segment.pipeline_id, @segment.id) %> | 
<%= link_to 'Back', pipeline_path(@pipeline) %>

<%= render 'index' %>
