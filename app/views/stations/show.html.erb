<p id="notice"><%= notice %></p>
<%= render 'pipelines/show_one' %>
<%= render 'show_one' %>

<nav class="navbar navbar-default small">
  <div class="container-fluid">
	<ul class="nav navbar-nav">
		<li><%= link_to 'Edit Station', edit_pipeline_station_path(@station.pipeline_id, @station.id) %> </li>
		<li><%= link_to 'Delete', pipeline_station_path(@station.pipeline_id, @station.id), method: :delete, data: { confirm: 'Are you sure you want to delete this station?' } %> </li>
		<li><%= link_to 'Back', pipeline_path(@pipeline) %> </li>
	</ul>
  </div>
</nav>
<%= render 'units/index' %>




