<%= render 'pipelines/show_one' %>
<br>
<%= render 'index' %>
<%= render 'form' %>

<%= link_to 'Show', pipeline_station_path(@station.pipeline_id, @station.id) %> | 
<%= link_to 'Back', pipeline_path(@pipeline) %>
