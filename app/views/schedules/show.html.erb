<p id="notice"><%= notice %></p>
<%= render 'show_one' %>

<%= link_to 'Edit Schedule', edit_schedule_path(@schedule) %> | 
<%= link_to 'Delete', schedule_path(@schedule.id), method: :delete, data: { confirm: 'Are you sure you want to delete this schedule?' } %> |
<%= link_to 'Back', schedules_path %>

<%= render 'activities/index' %>

