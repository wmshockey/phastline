<p id="notice"><%= notice %></p>

<%= render 'nominations/show_one' %>
<br>
<%= render 'index' %>
<h2>Shipment</h2>
<table class="fixed", style="width: 1200px;">
  <thead>
    <tr>
	  <th>Start Location</th>
      <th>End Location</th>
      <th>Shipper</th>
      <th>Commodity ID</th>
      <th>Volume</th>
    </tr>
  </thead>

  <tbody>
      <tr>
		<td><%= @shipment.start_location %></td>
		<td><%= @shipment.end_location %></td>
		<td><%= @shipment.shipper %></td>
		<td><%= @shipment.commodity_id %></td>
		<td><%= @shipment.volume %></td>
      </tr>
  </tbody>
</table>

<%= link_to 'Edit', edit_nomination_shipment_path(@shipment.nomination_id, @shipment.id) %> |
<%= link_to 'Delete', nomination_shipment_path(@shipment.nomination_id, @shipment.id), method: :delete, data: { confirm: 'Are you sure you want to delete this shipment?' } %> |
<%= link_to 'Back', nomination_path(@nomination) %>