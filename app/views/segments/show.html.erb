<%= render 'pipelines/show_one' %>
<%= render 'show_one'%>

<div class="col-sm-2"></div>
<div class="col-sm-10">
<nav class="navbar navbar-default">
  <div class="container-fluid">
	<ul class="nav navbar-nav">
		<li><%= link_to 'Edit Segment', edit_pipeline_segment_path(@segment.pipeline_id, @segment.id) %> </li>
		<li><%= link_to 'Delete', pipeline_segment_path(@segment.pipeline_id, @segment.id), method: :delete, data: { confirm: 'Are you sure you want to delete this segment?' } %> </li>
		<li><%= link_to 'Back', :back %> </li>
	</ul>
  </div>
</nav>
</div>